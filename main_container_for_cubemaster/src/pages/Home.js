import React from 'react';
import CubeMaster from '../components/CubeMaster';

// PUBLIC_INTERFACE
/**
 * Home page component that renders the CubeMaster main container
 */
const Home = () => {
  return <CubeMaster />;
};

export default Home;
